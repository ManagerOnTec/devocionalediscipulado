-r base.txt

# ── Extras exclusivos de produção ────────────────────────────────────────────

# Monitoramento de erros
sentry-sdk[django]>=1.45

# Parse de DATABASE_URL (Neon / Cloud SQL via connection string)
dj-database-url>=2.1
